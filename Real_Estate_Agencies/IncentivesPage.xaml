<Page x:Class="Real_Estate_Agencies.IncentivesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Incentives">

    <Grid Background="White" Margin="20">
        <StackPanel>
            <TextBlock Text="Incentives"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#333"
                       Margin="0 0 0 10"/>

            <DataGrid AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      Height="400"
                      RowBackground="White"
                      AlternatingRowBackground="#FFF8DC"
                      GridLinesVisibility="None"
                      BorderBrush="#E0E0E0"
                      BorderThickness="1"
                      FontSize="14"
                      Foreground="#333">

                <!-- Header Styling -->
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F9E2AE"/>
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="8"/>
                        <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Incentive ID" Width="*" Binding="{Binding IncentiveID}"/>
                    <DataGridTextColumn Header="Agent ID" Width="*" Binding="{Binding AgentID}"/>
                    <DataGridTextColumn Header="Sale ID" Width="*" Binding="{Binding SaleID}"/>
                    <DataGridTextColumn Header="Incentive Type" Width="*" Binding="{Binding IncentiveType}"/>
                    <DataGridTextColumn Header="Reward/Amount" Width="*" Binding="{Binding Reward}"/>
                    <DataGridTextColumn Header="Status" Width="*" Binding="{Binding Status}"/>
                    <DataGridTextColumn Header="Date" Width="*" Binding="{Binding Date}"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Page>
