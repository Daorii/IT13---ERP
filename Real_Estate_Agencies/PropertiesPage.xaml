<Page x:Class="Real_Estate_Agencies.PropertiesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:Real_Estate_Agencies"
      Title="PropertiesPage"
      Background="#F5F5F5">

    <Page.Resources>
        <!-- Card style -->
        <Style x:Key="PropertyCardStyle" TargetType="Border">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="8" Opacity="0" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)" To="0.3" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="Effect.(DropShadowEffect.Opacity)" To="0" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <DockPanel Margin="20">
        <!-- Header -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
            <Button Content="Add" Width="80" Background="#4CAF50" Foreground="White" FontWeight="Bold" Click="Add_Click" Margin="0,0,10,0"/>
            <Button Content="Edit" Width="80" Background="#FFC107" Foreground="White" FontWeight="Bold" Click="Edit_Click" Margin="0,0,10,0"/>
            <Button Content="Delete" Width="80" Background="#F44336" Foreground="White" FontWeight="Bold" Click="Delete_Click"/>
        </StackPanel>

        <!-- Property List -->
        <ListBox x:Name="PropertiesList" ItemsSource="{Binding Properties}" 
                 HorizontalContentAlignment="Stretch" BorderThickness="0" Background="Transparent" SelectionMode="Single">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource PropertyCardStyle}" 
                            Background="White" CornerRadius="15" Margin="6" Padding="10"
                            MinWidth="200" MaxWidth="280" MinHeight="250" MaxHeight="320" BorderBrush="#E0E0E0" BorderThickness="1">
                        <StackPanel>
                            <Border CornerRadius="10" BorderThickness="1" BorderBrush="#DDD" Margin="0,0,0,8" Height="120">
                                <Image Source="{Binding ImagePath}" Stretch="UniformToFill" ClipToBounds="True" />
                            </Border>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                            <TextBlock Text="{Binding Location}" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding PropertyType}" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding Category}" FontSize="12" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding Price, StringFormat=C}" FontSize="12" Margin="0,2,0,0"/>
                            <Border Background="#F0F0F0" CornerRadius="8" Padding="4" HorizontalAlignment="Left" Margin="0,8,0,0">
                                <TextBlock Text="{Binding Status}" FontSize="12" Foreground="#333"/>
                            </Border>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Page>
