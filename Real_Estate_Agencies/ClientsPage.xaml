<Page x:Class="Real_Estate_Agencies.ClientsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="ClientsPage"
      Background="#F8FAFC">

    <Page.Resources>

        <DropShadowEffect x:Key="DropShadowEffect"
                  Color="#000000"
                  BlurRadius="15"
                  ShadowDepth="3"
                  Opacity="0.25"/>

        <SolidColorBrush x:Key="PrimaryBlue" Color="#3B82F6"/>
        <SolidColorBrush x:Key="DarkBlue" Color="#1E40AF"/>
        <SolidColorBrush x:Key="Gray50" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="Gray100" Color="#F3F4F6"/>
        <SolidColorBrush x:Key="Gray200" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="Gray400" Color="#9CA3AF"/>
        <SolidColorBrush x:Key="Gray600" Color="#4B5563"/>
        <SolidColorBrush x:Key="Gray800" Color="#1F2937"/>
        <SolidColorBrush x:Key="PastelBlue" Color="#A7C7E7"/>
        <SolidColorBrush x:Key="PastelYellow" Color="#FFF9B1"/>
        <SolidColorBrush x:Key="GlassPastelYellow" Color="#FFF9B1" Opacity="0.6"/>
        <SolidColorBrush x:Key="ButtonShadow" Color="#000000"/>

        <DropShadowEffect x:Key="SoftButtonShadow"
                      Color="#000000"
                      BlurRadius="12"
                      ShadowDepth="2"
                      Opacity="0.25"/>

        <!-- Rounded TextBox (Glassmorphism Style) -->
        <Style x:Key="RoundedTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background">
                <Setter.Value>
                    <SolidColorBrush Color="#FFF9B1" Opacity="0.7"/>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#E1E8ED"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#5C677D"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" BlurRadius="6" Opacity="0.15"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#4F46E5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Shadowy Button Style (same radius as Add Client) -->
        <Style x:Key="ShadowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Effect" Value="{StaticResource SoftButtonShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3730A3"/>
                </Trigger>
            </Style.Triggers>
        </Style>


        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource Gray600}"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CellTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource Gray800}"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="CircularIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource Gray50}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource Gray400}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AddButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DarkBlue}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FormComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource Gray800}"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"
                                          Content="{TemplateBinding SelectionBoxItem}"/>

                                <ToggleButton Grid.Column="1"
                                      Width="30"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      Focusable="False"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                      ClickMode="Press">
                                    <Path Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="{StaticResource Gray600}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                                </ToggleButton>

                                <Popup x:Name="PART_Popup"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               AllowsTransparency="True">
                                    <Border Background="White"
                                    BorderBrush="{StaticResource Gray200}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    MaxHeight="200">
                                        <ScrollViewer>
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FormTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource Gray800}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Border Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            
            <!-- Page Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Clients" FontSize="28" FontWeight="Bold" Foreground="{StaticResource Gray800}"/>
            </StackPanel>

            <!-- Header + Search -->
            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Client Search Bar -->
                <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="{StaticResource Gray200}" BorderThickness="1" Margin="0,0,12,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Search Icon -->
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="14" Margin="12,0,8,0" VerticalAlignment="Center" Foreground="{StaticResource Gray400}"/>

                        <!-- Search TextBox -->
                        <TextBox Grid.Column="1" x:Name="SearchTextBox" 
                 Background="Transparent" BorderThickness="0" FontSize="14" Padding="0,12" 
                 VerticalAlignment="Center" Foreground="{StaticResource Gray800}"
                 TextChanged="SearchTextBox_TextChanged"/>

                        <!-- Placeholder Text -->
                        <TextBlock Grid.Column="1" Text="Search clients..." x:Name="PlaceholderText" 
                   Foreground="{StaticResource Gray400}" IsHitTestVisible="False" 
                   VerticalAlignment="Center" Margin="0,12"/>
                    </Grid>
                </Border>

                <!-- Add Client Button -->
                <Button Grid.Column="1" Content="+ Add Client" Style="{StaticResource AddButtonStyle}" Click="AddClient_Click" Margin="16,0,0,0"/>
            </Grid>

            <!-- Clients Table -->
            <Grid Grid.Row="2" Grid.RowSpan="2" HorizontalAlignment="Stretch">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Table Header -->
                <Border Background="White" CornerRadius="8,8,0,0" BorderBrush="{StaticResource Gray200}" BorderThickness="1,1,1,0" Grid.Row="0">
                    <Grid Background="{StaticResource Gray50}" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Client ID" Style="{StaticResource HeaderTextStyle}" TextAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="First Name" Style="{StaticResource HeaderTextStyle}" TextAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="Last Name" Style="{StaticResource HeaderTextStyle}" TextAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="Contact Info" Style="{StaticResource HeaderTextStyle}" TextAlignment="Center"/>
                        <TextBlock Grid.Column="4" Text="Address" Style="{StaticResource HeaderTextStyle}" TextAlignment="Center"/>
                        <TextBlock Grid.Column="5" Text="Action" Style="{StaticResource HeaderTextStyle}" TextAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Clients List -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="White">
                    <Border CornerRadius="0,0,8,8" BorderBrush="{StaticResource Gray200}" BorderThickness="1,0,1,1">
                        <ItemsControl x:Name="ClientsDataGrid" ItemsSource="{Binding Clients}">
                            <ItemsControl.ItemsPanel>
                                <!-- Use a Grid for each row to align columns -->
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" BorderBrush="{StaticResource Gray100}" BorderThickness="0,0,0,1">
                                        <Grid MouseLeftButtonDown="ClientRow_Click" HorizontalAlignment="Stretch">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1*"/>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="3*"/>
                                                <ColumnDefinition Width="2*"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding ClientId}" Style="{StaticResource CellTextStyle}" TextAlignment="Center"/>
                                            <TextBlock Grid.Column="1" Text="{Binding FirstName}" Style="{StaticResource CellTextStyle}" TextAlignment="Center"/>
                                            <TextBlock Grid.Column="2" Text="{Binding LastName}" Style="{StaticResource CellTextStyle}" TextAlignment="Center"/>
                                            <TextBlock Grid.Column="3" Text="{Binding ContactInfo}" Style="{StaticResource CellTextStyle}" TextAlignment="Center"/>
                                            <TextBlock Grid.Column="4" Text="{Binding Address}" Style="{StaticResource CellTextStyle}" TextAlignment="Center"/>

                                            <StackPanel Grid.Column="5" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Button Style="{StaticResource CircularIconButtonStyle}" ToolTip="Edit" Margin="2,0" Click="EditClient_Click" CommandParameter="{Binding}">
                                                    <TextBlock Text="✏️" FontSize="12"/>
                                                </Button>
                                                <Button Style="{StaticResource CircularIconButtonStyle}" ToolTip="Delete" Margin="2,0" Click="DeleteClient_Click" CommandParameter="{Binding}">
                                                    <TextBlock Text="🗑️" FontSize="12"/>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </ScrollViewer>
            </Grid>


            <!-- Edit Client Overlay -->
            <!-- =============== EDIT CLIENT POPUP =============== -->
            <Grid x:Name="EditPopupOverlay"
      Grid.RowSpan="4"
      Background="#80000000"
      Visibility="Collapsed"
      VerticalAlignment="Stretch"
      HorizontalAlignment="Stretch"
      Panel.ZIndex="99">

                <Border x:Name="EditClientPanel"
            Background="White"
            CornerRadius="16"
            Padding="30"
            Width="480"
            MaxHeight="700"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Margin="20"
            Opacity="0"
            RenderTransformOrigin="0.5,0.5">
                    <Border.RenderTransform>
                        <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                    </Border.RenderTransform>

                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.12" BlurRadius="16" ShadowDepth="4" Direction="270"/>
                    </Border.Effect>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <StackPanel Grid.Row="0" Margin="0,10,0,20">
                            <TextBlock Text="Edit Client" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                            <TextBlock Text="Update client information" FontSize="13" Foreground="#6C7A89" Margin="0,5,0,0"/>
                        </StackPanel>

                        <!-- Form Content -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel Orientation="Vertical">
                                <TextBox x:Name="EditClientIdTextBox" Visibility="Collapsed"/>

                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="First Name" FontSize="13" FontWeight="SemiBold" Foreground="#5C677D"/>
                                    <TextBox x:Name="EditFirstNameTextBox" Height="40" Padding="10"
                                 BorderBrush="#E1E8ED" BorderThickness="1"
                                 Background="White" Foreground="#2C3E50"
                                 VerticalContentAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="Last Name" FontSize="13" FontWeight="SemiBold" Foreground="#5C677D"/>
                                    <TextBox x:Name="EditLastNameTextBox" Height="40" Padding="10"
                                 BorderBrush="#E1E8ED" BorderThickness="1"
                                 Background="White" Foreground="#2C3E50"
                                 VerticalContentAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="Contact Info" FontSize="13" FontWeight="SemiBold" Foreground="#5C677D"/>
                                    <TextBox x:Name="EditContactInfoTextBox" Height="40" Padding="10"
                                 BorderBrush="#E1E8ED" BorderThickness="1"
                                 Background="White" Foreground="#2C3E50"
                                 VerticalContentAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Margin="0,0,0,12">
                                    <TextBlock Text="Address" FontSize="13" FontWeight="SemiBold" Foreground="#5C677D"/>
                                    <TextBox x:Name="EditAddressTextBox" Height="70" Padding="10"
                                 BorderBrush="#E1E8ED" BorderThickness="1"
                                 Background="White" Foreground="#2C3E50"
                                 TextWrapping="Wrap" AcceptsReturn="True"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Footer Buttons -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,20,0,0">

                            <!-- Close Button -->
                            <Button Content="Close"
                        Width="90" Height="40"
                        Background="White"
                        BorderBrush="#D0D5DD"
                        BorderThickness="1"
                        Foreground="#344054"
                        Cursor="Hand"
                        Margin="0,0,12,0"
                        Click="CloseEditPopup_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border CornerRadius="8"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Padding" Value="10,0"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#F2F4F7"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Save Client Button -->
                            <Button Content="Save Changes"
                        Width="150" Height="40"
                        Background="#4F46E5"
                        BorderThickness="0"
                        Foreground="White"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="SaveClient_Click">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border CornerRadius="8"
                                                Background="{TemplateBinding Background}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#6366F1"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#4338CA"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>






            <!-- Profile Popup Overlay -->
            <!-- Client Profile Popup Modal -->

            <Grid x:Name="ClientDetailsOverlay" Grid.RowSpan="3"
      Background="#80FFFFFF"
      Visibility="Collapsed"
      VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Panel.ZIndex="98">

                <Border Background="#F9FAFB" CornerRadius="20" Padding="25"
            Width="520" MaxHeight="680"
            VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="25" ShadowDepth="5"/>
                    </Border.Effect>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <TextBlock Text="CLIENT INFORMATION"
                       FontSize="22" FontWeight="Bold" Foreground="#1F2937"
                       TextAlignment="Center" Margin="0,0,0,18"/>

                        <!-- Client Info Section -->
                        <StackPanel Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center">

                            <!-- Full Name -->
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,8">
                                <TextBlock Text="👤" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Full Name:" FontWeight="SemiBold" Foreground="#374151" Width="120"/>
                                <TextBlock x:Name="ProfileFullName" Foreground="#111827"/>
                            </StackPanel>

                            <!-- Contact Info -->
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,8">
                                <TextBlock Text="☎️" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Contact Info:" FontWeight="SemiBold" Foreground="#374151" Width="120"/>
                                <TextBlock x:Name="ProfileContactInfo" Foreground="#111827"/>
                            </StackPanel>

                            <!-- Address -->
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,8">
                                <TextBlock Text="📍" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Address:" FontWeight="SemiBold" Foreground="#374151" Width="120"/>
                                <TextBlock x:Name="ProfileAddress" Foreground="#111827" TextWrapping="Wrap" Width="300"/>
                            </StackPanel>

                            <Rectangle Fill="#E5E7EB" Height="1" Width="440" HorizontalAlignment="Center" Margin="0,12,0,12"/>

                            <!-- Balance -->
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,8">
                                <TextBlock Text="💰" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Balance:" FontWeight="SemiBold" Foreground="#374151" Width="120"/>
                                <TextBlock x:Name="ProfileBalance" Foreground="#111827"/>
                            </StackPanel>

                          

                            <!-- Payment Type -->
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,8">
                                <TextBlock Text="💳" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Payment Type:" FontWeight="SemiBold" Foreground="#374151" Width="120"/>
                                <TextBlock x:Name="ProfilePaymentType" Foreground="#111827"/>
                            </StackPanel>

                            <!-- Status -->
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,8">
                                <TextBlock Text="🔘" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Status:" FontWeight="SemiBold" Foreground="#374151" Width="120"/>
                                <TextBlock x:Name="ProfileStatus" Foreground="#111827"/>
                            </StackPanel>

                        </StackPanel>

                        <Rectangle Grid.Row="1" Fill="#E5E7EB" Height="1" Width="440"
                       HorizontalAlignment="Center" Margin="0,15,0,15"/>

                        <!-- Close Button -->
                        <Border Grid.Row="2"
        Width="140" Height="40" CornerRadius="20"
        Background="#2563EB" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.25" BlurRadius="10" ShadowDepth="2"/>
                            </Border.Effect>
                            <Button Content="Close"
            Background="Transparent" Foreground="White"
            BorderThickness="0" FontWeight="Bold" FontSize="14"
            Cursor="Hand" Click="CloseProfilePopup_Click"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- Client Table Pagination -->
            <Grid Grid.Row="3" Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Info Text -->
                <TextBlock Grid.Column="0" Text="Showing 1-10 of clients"
               VerticalAlignment="Center" Foreground="{StaticResource Gray600}" 
               FontSize="12" HorizontalAlignment="Left"/>

                <!-- Page Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="❮" Style="{StaticResource CircularIconButtonStyle}" Margin="2,0"/>
                    <Button Content="1" Background="{StaticResource PrimaryBlue}" Foreground="White"
                Style="{StaticResource CircularIconButtonStyle}" Margin="2,0"/>
                    <Button Content="❯" Style="{StaticResource CircularIconButtonStyle}" Margin="2,0"/>
                </StackPanel>
            </Grid>






        </Grid>
    </Border>
</Page>
